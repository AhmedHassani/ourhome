
class Statistics {
     String ? successOrder;
     String ? failOrder;
     String ? totalPrice;
     String ? totalOrder;
     String ? filed;

     Statistics({
     required this.successOrder, required this.failOrder, required this.totalPrice, required this.totalOrder,required this.filed});
}